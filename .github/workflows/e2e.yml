name: E2E Tests

on:
  push:
    branches: [7.0]
  pull_request:
    branches: [7.0]

jobs:
  e2e:
    name: E2E Tests (Neos 8)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version-file: Tests/E2E/.nvmrc
          cache: npm
          cache-dependency-path: Tests/E2E/package-lock.json

      - name: Install dependencies
        working-directory: Tests/E2E
        run: npm ci

      - name: Install Playwright browsers
        working-directory: Tests/E2E
        run: npx playwright install --with-deps chromium

      - name: Pre-build Docker image
        run: docker compose -f Tests/system_under_test/neos8/docker-compose.yaml build --pull

      - name: Test - registration
        working-directory: Tests/E2E
        run: npm run test:registration

      - name: Test - login
        working-directory: Tests/E2E
        run: npm run test:login

      - name: Test - reset password
        working-directory: Tests/E2E
        run: npm run test:reset-password

      - name: Test - profile
        working-directory: Tests/E2E
        run: npm run test:profile

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: Tests/E2E/playwright-report/
          retention-days: 7
